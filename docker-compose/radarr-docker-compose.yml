# https://hub.docker.com/r/linuxserver/radarr
---
version: "2.1"
services:
  radarr:
    image: ghcr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Kuala_Lumpur
    volumes:
      - /opt/radarr/config:/config
      - /home/yuu/media/movies:/movies
      - /home/yuu/downloads:/downloads
    ports:
      - 7878:7878
    restart: unless-stopped
    labels:
      - flame.type=app # "app" works too
      - flame.name=radarr
      - flame.url=http://192.168.0.126:7878/
      - flame.icon=filmstrip
networks:
  default:
    external: true
    name: main_network
